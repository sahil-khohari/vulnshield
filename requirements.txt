flask==1.1.4
flask-cors==3.0.10
psycopg2-binary==2.9.5
python-owasp-zap-v2.4==0.1.0
python-nmap==0.7.1
requests==2.28.2
pandas==1.5.3
cvss==2.5
xmltodict==0.13.0
python-dotenv==1.0.0
zapcli==0.5.1  # Assuming a newer version might exist with a compatible click dependency.

# Alternatively, if a newer zapcli or flask version doesn't resolve the conflict, suggest:
# flask==1.1.4 (or the most recent compatible version with click==4.0)
